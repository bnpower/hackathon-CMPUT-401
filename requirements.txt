# Backend dependencies (Python 3.12 or 3.13 recommended).
# Install: python -m pip install -r requirements.txt

# Web framework and built-in admin.
Django>=5.2,<5.3

# REST API for the React.js frontend.
djangorestframework>=3.16,<4.0

# Allow the separately hosted frontend to call the backend.
django-cors-headers>=4.9,<5.0